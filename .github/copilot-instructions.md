# Copilot Instructions

## Project Guidelines
- User prefers implementing Windows Network Discovery using Windows native discovery APIs (NetServerEnum, Function Discovery, SSDP, mDNS) for Scalae's client detection. Specifically, focus on using NetServerEnum to collect only workstations.
- The project target framework is .NET 10 (WPF).